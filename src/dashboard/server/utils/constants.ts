export { OFFLINE_GRACE_PERIOD_MS } from '../../../allocation/reassignOffline';

// Fixed for MVP — not surfaced as a setting yet, see design spec section 7.
export const WAITING_STUCK_THRESHOLD_MS = 2 * 60 * 1000;
